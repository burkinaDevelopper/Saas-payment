<?php
use App\Models\User;

// declare(stripe_types=1)
// // description:
// if ('can subscribe',function(){
//     $stripe = new StripeClient(config('stripe.stripe_secret'));
//     $user=User::factory()->create();
// })->only();